part of 'main_view.dart';

class _MainAdaptive extends StatelessWidget {
  const _MainAdaptive(this.viewModel);

  final MainViewModel viewModel;

  @override
  Widget build(BuildContext context) {
    return const HomeView();
  }
}
