part of 'bookings_view.dart';

class _BookingsAdaptive extends StatelessWidget {
  const _BookingsAdaptive(this.viewModel);

  final BookingsViewModel viewModel;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
