import 'dart:ui';

class AppConstants {
  AppConstants._internal();
  static const String appName = 'Hang Meas';

  static const Locale fallbackLocale = Locale('en', 'US');

  static const supportedLocales = [
    Locale('en'),
    Locale('km'),
  ];
}
